SECRET_KEY=django-insecure-change-me-in-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# --- Database ---
# SQLite (geliştirme): boş bırakın, varsayılan kullanılır
# PostgreSQL (production): DATABASE_URL ortam değişkeni Railway tarafından otomatik set edilir
DATABASE_URL=

# --- E-posta (Gmail SMTP) ---
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password   # Gmail → Güvenlik → Uygulama Şifreleri

# --- Railway / Production ---
# ALLOWED_HOSTS=your-domain.up.railway.app
# DEBUG=False
